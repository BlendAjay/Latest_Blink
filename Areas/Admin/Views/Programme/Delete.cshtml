@model AJSolutions.Models.ProgrammeMaster

@{
    ViewBag.Title = "Delete";
}
<div class="container">
    <div id="contact-form-overlay" class="clearfix">
        <div class="row">
            <div class="fancy-title title-bottom-border">
                <h3>Are you sure you want to delete this?</h3>
            </div>
            <div class="col_half">
                <b> @Html.DisplayNameFor(model => model.ProgrammeName)</b>
                @Html.DisplayFor(model => model.ProgrammeName)
            </div>
            <div class="col_half col_last">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    <input type="submit" value="Delete" class="button button-3d button-rounded button-teal" style="float:right" />
                }
            </div>
        </div>
        <p>
            @Html.ActionLink("Back to List", "Index")
        </p>
    </div>
</div>